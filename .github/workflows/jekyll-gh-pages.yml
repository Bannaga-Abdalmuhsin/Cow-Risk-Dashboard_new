name: Jekyll (Disabled)

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: "This workflow is disabled. The project uses deploy-gh-pages.yml instead."
        required: false

jobs:
  placeholder:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled"
